<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dq.house.web.admin.mapper.FacilityInfoMapper">
    <!--    根据公寓id删除设施关联关系-->
    <update id="removeByApartmentId">
        update apartment_facility
        set is_deleted = 1
        where apartment_id = #{id}
    </update>
    <!--    根据公寓id获取设施列表-->
    <select id="getListByApartmentId" resultType="com.dq.house.model.entity.FacilityInfo">
        select fi.id, fi.type, fi.name, fi.icon
        from facility_info fi
        where fi.id in (
            (select facility_id
             from apartment_facility af
             where is_deleted = 0
               and apartment_id = #{id}))
          and fi.is_deleted = 0
    </select>

</mapper>
