<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dq.house.web.admin.mapper.RoomPaymentTypeMapper">
    <!--    根据房间id查询缴费类型列表-->
    <select id="getListByRoomId" resultType="com.dq.house.model.entity.PaymentType">
        select id, name, pay_month_count, additional_info, is_deleted
        from payment_type
        where is_deleted = 0
          and id in
              (select payment_type_id
               from room_payment_type
               where is_deleted = 0
                 and room_id = #{id})
    </select>
</mapper>
