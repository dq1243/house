<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dq.house.web.admin.mapper.RoomInfoMapper">
    <resultMap id="roomList" type="com.dq.house.web.admin.vo.room.RoomItemVo">
        <result property="id" column="roomId"/>
        <result property="roomNumber" column="room_number"/>
        <result property="rent" column="rent"/>
        <result property="apartmentId" column="apartment_id"/>
        <result property="isRelease" column="is_release"/>
        <result property="isDeleted" column="is_deleted"/>
        <result property="leaseEndDate" column="lease_end_date"/>
        <result property="isCheckIn" column="is_check_in"/>
        <association property="apartmentInfo" javaType="com.dq.house.model.entity.ApartmentInfo">
            <id property="id" column="apartment_id"/>
            <result property="name" column="name"/>
            <result property="phone" column="phone"/>
            <result property="introduction" column="introduction"/>
            <result property="latitude" column="latitude"/>
            <result property="longitude" column="longitude"/>
            <result property="districtId" column="district_id"/>
            <result property="districtName" column="district_name"/>
            <result property="cityId" column="city_id"/>
            <result property="cityName" column="city_name"/>
            <result property="provinceId" column="province_id"/>
            <result property="provinceName" column="province_name"/>
            <result property="addressDetail" column="address_detail"/>
            <result property="isRelease" column="is_release"/>
            <result property="isDeleted" column="is_deleted"/>
        </association>
    </resultMap>
    
    <select id="pageItem" resultMap="roomList">
        select ri.id roomId,
        ri.room_number,
        ri.apartment_id,
        ri.rent,
        ri.is_release,
        ri.is_deleted,
        ifnull(la.is_check_in, 0) is_check_in,
        la.lease_end_date,
        la.apartment_id,
        ai.name,
        ai.introduction,
        ai.district_id,
        ai.district_name,
        ai.city_id,
        ai.city_name,
        ai.province_id,
        ai.province_name,
        ai.address_detail,
        ai.latitude,
        ai.longitude,
        ai.phone,
        ai.is_release,
        ai.is_deleted
        from room_info
        ri
        left join
        (select id,
        name,
        introduction,
        district_id,
        district_name,
        city_id,
        city_name,
        province_id,
        province_name,
        address_detail,
        latitude,
        longitude,
        phone,
        is_release,
        is_deleted
        from apartment_info
        where is_deleted = 0)
        ai
        on ri.apartment_id = ai.id
        left join
        (select id,
        rent,
        room_id,
        apartment_id,
        is_deleted,
        lease_start_date,
        lease_end_date,
        if(lease_end_date > now(), 1, 0) is_check_in
        from lease_agreement
        where is_deleted = 0)
        la
        on la.apartment_id = ai.id
        and la.room_id = ri.id
        <where>
            ri.is_deleted = 0
            <if test="queryVo.provinceId != null">and ai.province_id = #{queryVo.provinceId}</if>
            <if test="queryVo.cityId != null">and ai.city_id = #{queryVo.cityId}</if>
            <if test="queryVo.districtId != null">and ai.province_id = #{"queryVo.districtId}</if>
        </where>
    </select>
</mapper>
